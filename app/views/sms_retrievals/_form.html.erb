<%= form_for(@sms_retrieval) do |f| %>
  <% if @sms_retrieval.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sms_retrieval.errors.count, "error") %> prohibited this sms_retrieval from being saved:</h2>

      <ul>
      <% @sms_retrieval.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :phone_number %><br>
    <%= f.text_field :phone_number %>
  </div>
  <div class="field">
    <%= f.label :room_id %><br>
    <%= f.text_field :room_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
